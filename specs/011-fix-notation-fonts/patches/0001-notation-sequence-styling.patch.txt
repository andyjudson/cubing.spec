# Patch proposal: notation sequence/triggers styling
# File: cfop-app/src/pages/notation/Notation.css (example target)

Rationale:
- The current sequence/triggers styling uses a hard red or blank state which reduces readability.
- Propose softer-accessible colors and clear on/off state with subtle background and border.

*** Begin Patch
*** Update File: cfop-app/src/pages/notation/Notation.css
@@
-.notation-sequence .trigger.off {
-  color: #c42525; /* harsh red */
-  background: transparent;
-}
-.notation-sequence .trigger.on {
-  color: #ffffff;
-  background: #c42525;
-}
+.notation-sequence .trigger {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  padding: 0.25rem 0.5rem;
+  border-radius: 6px;
+  border: 1px solid transparent;
+  transition: all 0.15s ease;
+}
+
+.notation-sequence .trigger.off {
+  color: var(--color-text-tertiary);
+  background: rgba(15,23,42,0.03);
+  border-color: rgba(15,23,42,0.04);
+}
+
+.notation-sequence .trigger.on {
+  color: var(--color-bg-base);
+  background: linear-gradient(180deg, #ef4444 0%, #dc2626 100%); /* accessible red */
+  border-color: rgba(220,38,38,0.12);
+}
+
+.notation-sequence .trigger:focus {
+  outline: 2px solid rgba(37,99,235,0.18);
+  outline-offset: 2px;
+}
*** End Patch

Notes:
- Uses design tokens where possible; adjust color tokens if you want to centralize into `index.css`.
- Keeps semantics: `.trigger.on` = active, `.trigger.off` = inactive; colors chosen for contrast.
